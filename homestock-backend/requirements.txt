fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
pgvector==0.2.5
pydantic==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.9
sentence-transformers==2.7.0
websockets==12.0
pillow==10.3.0
# Sync HTTP client used by the wine enrichment service to call the local
# Ollama daemon. Stdlib `urllib` would work too, but httpx is much cleaner
# for JSON requests and gives us a single dependency that's easy to swap.
httpx==0.27.0
